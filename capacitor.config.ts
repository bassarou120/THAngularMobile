import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.talent.heroes',
  appName: 'TalentsHeroes',
  webDir: 'dist/talent-heroes-angular',
  bundledWebRuntime: false
};

export default config;
